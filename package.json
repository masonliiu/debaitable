{
  "name": "debaitable",
  "version": "0.0.2",
  "description": "Terminal-based multi-role debate engine for structured decisions.",
  "license": "UNLICENSED",
  "type": "module",
  "bin": {
    "debaitable": "dist/main.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "dev": "tsx src/dev.ts",
    "cli": "tsx src/cli/main.ts",
    "dev:web": "sh -c 'if [ -f web/package.json ]; then npm --prefix web run dev; else echo \"web/ not set up yet\"; fi'",
    "build:cli": "tsup src/cli/main.ts --format esm --target node18 --out-dir dist --clean",
    "build:web": "sh -c 'if [ -f web/package.json ]; then npm --prefix web run build; else echo \"web/ not set up yet\"; fi'",
    "build": "npm run build:cli",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "prepublishOnly": "npm run typecheck && npm run build:cli"
  },
  "dependencies": {
    "@types/blessed": "^0.1.27",
    "blessed": "^0.1.81",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "tsup": "^8.5.1",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
